package com.test.example

import kotlin.test.Test

class LocalAlignTest {
    @Test
    fun test() {
        /*
        assertEquals("TOP", LocalAlign.TOP)
        assertEquals("LEFT", LocalAlign.LEFT)
        assertEquals("BOTTOM", LocalAlign.BOTTOM)
        assertEquals("RIGHT", LocalAlign.RIGHT)
        */
    }
}
